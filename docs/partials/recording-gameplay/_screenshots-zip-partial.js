const text = `

This .zip contains numbered JPG files, each representing a screenshot taken at a specific tick.
These are used in the Regression Games web interface to provide context around game state data by
displaying what was happening on-screen at a given moment in time.

`;

export default text;