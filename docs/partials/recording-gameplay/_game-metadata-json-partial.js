const text = `

This .json file contains metadata about your game such as its title, studio/publisher, editor version,
and which input systems and rendering pipelines it utilizes.
This information can be useful to Regression Games for debugging compatibility issues 
between our SDK and your game.

`;

export default text;