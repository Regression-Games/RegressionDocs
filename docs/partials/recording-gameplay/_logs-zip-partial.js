const text = `

This .zip contains numbered JSONL files, each containing runtime logs at a specific tick.
A file contains any logs produced since the previous tick.
The file may be empty if no logs were produced in that time.

`;

export default text;